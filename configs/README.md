# Configs

This directory stores project configuration that can safely live outside the repository root.

Root-level files that external platforms expect, such as `package.json`, `vercel.json`, and `.env*`, stay at the root.
