# Tests

This directory stores lightweight project tests.

Current scripts:

- `npm test`: runs `tests/smoke.mjs`.
- `npm run test:smoke`: same smoke test entrypoint.

The smoke test checks that the new workspace seed is empty, legacy `mock-data` references are gone, and frontend source copy remains global English.
